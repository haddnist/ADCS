function d = stod(s)
    min = floor(s/60);
    hour = floor(s/(60*60));
    days = floor(s/(24*60*60));
    hour = hour - days*24;
    min = min - (days*24*60) - (hour*60);
    sec = s - (min*60) - (hour*60*60) - (days*24*60*60);
    
    if (days>=0 && days<31)
        month = 1;
        date = floor(days+1);
    elseif (days>=31 && days<60)
        month =2;
        date = floor(days+1)-31;
    elseif (days>=60 && days<91)
        month =3;
        date = floor(days+1)-60;
    elseif (days>=91 && days<121)
        month =4;
        date = floor(days+1)-91;
    elseif (days>=121 && days<152)
        month =5;
        date = floor(days+1)-121;
    elseif (days>=152 && days<182)
        month =6;
        date = floor(days+1)-152;
    elseif (days>=182 && days<213)
        month =7;
        date = floor(days+1)-182;
    elseif (days>=213 && days<244)
        month =8;
        date = floor(days+1)-213;
    elseif (days>=244 && days<274)
        month =9;
        date = floor(days+1)-244;
    elseif (days>=274 && days<305)
        month =10;
        date = floor(days+1)-274;
    elseif (days>=305 && days<335)
        month =11;
        date = floor(days+1)-305;
    elseif (days>=335 && days<366)
        month =12;
        date = floor(days+1)-335;
    end
    y=2024;
    d = [y month date hour min sec];
end